#include "Logger.h"

// Definition of the static member
bool Logger::debug_flag =
    true; // Initialize it with a default value (e.g., false)
bool Logger::verbose_flag =
    true; // Initialize it with a default value (e.g., false)
